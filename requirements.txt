# Dependencias de ejecucion del Forensic Auditor.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# El modelo NO se instala por aqui: corre en Ollama, fuera de Python.
# Ver scripts/setup_llm.sh.

# --- runtime del agente (etapas 1-6) ---
requests>=2.31            # unico cliente HTTP; habla con Ollama
scikit-learn>=1.3         # carga los .pkl del CART en src/scoring/model/
pandas>=2.0               # reconstruye el vector de features en inferencia
numpy>=1.24               # dependencia de las dos anteriores

# --- solo para reentrenar y graficar (ml/), no hace falta para correr ---
matplotlib>=3.7           # matrices de confusion y curvas ROC
